# Core data / ML stack
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10

# Classical NLP
spacy>=3.7,<3.9
nltk>=3.8
vaderSentiment>=3.3.2
wordcloud>=1.9
lightgbm>=4.0

# Plotting
matplotlib>=3.7
seaborn>=0.12

# I/O and notebooks
pyarrow>=12.0
jupyter>=1.0
notebook>=7.0

# Optional: enables the lemmatizer fallback when the spaCy model is missing.
# Not needed if en_core_web_sm is installed (recommended path below).
spacy-lookups-data>=1.0

# ---------------------------------------------------------------------------
# After installing the above, download the spaCy English model ONCE:
#
#     python -m spacy download en_core_web_sm
#
# The pipeline is built around en_core_web_sm. If it is unavailable, the code
# falls back to a blank English pipeline (sentencizer + lookup lemmatizer);
# results stay valid, lemmas are slightly less accurate. The NLTK POS tagger is
# used for the noun-phrase fallback in that case:
#
#     python -c "import nltk; nltk.download('averaged_perceptron_tagger_eng')"
# ---------------------------------------------------------------------------
